package com.example.visitorsystem.repository;

import com.example.visitorsystem.model.VisitorSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VisitorSystemRepository extends JpaRepository<VisitorSystem, Long> {
}
